import React, { Component } from 'react';

class savedBooks extends Component {

    // state = {
    //     svBooks: [],
    // }

    // componentDidMount() {
    //     API.getAllBooks()
    //         .then(svBooks => this.setState({ svBooks: svBooks }))
    //         .catch(err => console.error(err));
    // }
    render() {
        return (
            <div>
                
            </div>
        );
    }
}

export default savedBooks;